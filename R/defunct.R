#-------------------------------------------------------------------------------
".First.lib" <-
function(lib, pkg) {
   cat("nprq is a defunct package:  functionality now folded into the package quantreg")
}
